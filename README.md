## Maths

```ts
average([num1, num2, ...])
fixednum(num, digits)
minmax(num, min, max)
```

## Sounds

```ts
beep(type?, frequency?, duration?, volume?)
beepChain().beep().wait().beep()
```

## Misc

```ts
sleep(ms)
```
